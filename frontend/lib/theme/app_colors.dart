import 'package:flutter/material.dart';

/// Central color palette for the Kanban app.
class AppColors {
  AppColors._();

  static const accentYellow = Color(0xFFECAD0A);
  static const bluePrimary = Color(0xFF209DD7);
  static const purpleSecondary = Color(0xFF753991);
  static const darkNavy = Color(0xFF032147);
  static const grayText = Color(0xFF888888);

  static const background = Color(0xFFF4F6F9);
  static const surface = Colors.white;
}
